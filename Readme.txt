By projekt właściwie działał wymagane jest poprawienie ścieżek w dłączanych bibliotekach
1. dla Assemblera w pliku Form1.cs w lini 21 zmienić na właściwą ścieżkę do dll(z poprawną ścieżką do folderu na uruchamianym dysku)
2. dla C# w Zależności projektu JaAppForm dodać zależność dla dll C#(z poprawną ścieżką do folderu na uruchamianym dysku)